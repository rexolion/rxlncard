import * as React from 'react';

import './frontendParaContainerPara.css';

const FrontendParaContainerPara = () => (
    <p className="Frontend-para-para">The real barrier
    to the initial PDAs wasn’t the limits of software and hardware in a handheld box.
     It was the idea that the machine had to do nearly everything, that people couldn’t and wouldn’t want to learn.
     </p>
);

export default FrontendParaContainerPara;
