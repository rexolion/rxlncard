import React from 'react';

import './hero.css';
import HeroCard from './HeroCard/heroCard';

const Hero = () => (
    <section id="Hero" className="Hero-wrap">
        <HeroCard/>
    </section>
);

export default Hero;
